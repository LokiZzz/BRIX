@using BRIX.Lexica
@using BRIX.Library.Effects

Уменьшает максимальное количество очков действия у цели на @Numbers.RUSDcln(Model.Impact, "очко", "очка", "очков").

@code {
    [Parameter]
    public DecelerationEffect Model { get; set; } = new();
}
