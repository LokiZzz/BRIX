@using BRIX.Lexica
@using BRIX.Library.Aspects
@using BRIX.Library.Effects

Делает цели немыми на @Numbers.RUSDcln(Model.GetAspect<DurationAspect>
    ().Duration, "раунд", "раунда", "раундов"), если у них @Numbers.RUSDcln(Model.HealthThreshold, "очко", "очка", "очков") здоровья или меньше.

@code {
    [Parameter]
    public MutenessEffect Model { get; set; } = new();
}
