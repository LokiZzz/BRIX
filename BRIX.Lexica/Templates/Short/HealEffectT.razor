@using BRIX.Lexica
@using BRIX.Library.Aspects
@using BRIX.Library.Effects

Heal: @((MarkupString)Model.Impact.ToString()); @(Model.GetAspect<DurationAspect>().Duration == 0 ? "" : $" Periodic;")

@code {
    [Parameter]
    public HealEffect Model { get; set; } = new();
}