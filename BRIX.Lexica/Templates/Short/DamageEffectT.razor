@using BRIX.Lexica
@using BRIX.Library.Aspects
@using BRIX.Library.Effects

Dmg: @((MarkupString)Model.Impact.ToString()); @(Model.GetAspect<DurationAspect>().Duration == 0 ? "" : $" Periodic;")

@code {
    [Parameter]
    public DamageEffect Model { get; set; } = new();
}
