@using BRIX.Lexica
@using BRIX.Library.Effects

Temporarily increase the @((MarkupString)"target's") maximum health by @Numbers.ENGDcln(Model.Impact, "point").

@code {
    [Parameter]
    public FortifyEffect Model { get; set; } = new();
}
