@using BRIX.Lexica
@using BRIX.Library.Aspects
@using BRIX.Library.Effects

Creates terrain with difficulty class @Model.Impact.

@code {
    [Parameter]
    public DifficultTerrainEffect Model { get; set; } = new();
}
