@using BRIX.Lexica
@using BRIX.Library.Effects

The character accumulates incoming damage during @Numbers.ENGDcln(Model.Impact, "round") in order to return it to opponents later.

@code {
    [Parameter]
    public RevengeEffect Model { get; set; } = new();
}
