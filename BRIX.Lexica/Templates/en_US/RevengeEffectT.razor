@using BRIX.Lexica
@using BRIX.Library.Aspects
@using BRIX.Library.Effects

The character accumulates incoming damage during @Numbers.ENGDcln(Model.GetAspect<DurationAspect>().Duration, "round") in order to return it to opponents later.

@code {
    [Parameter]
    public RevengeEffect Model { get; set; } = new();
}
