@using BRIX.Lexica
@using BRIX.Library.Effects

Increases the @((MarkupString)"target's") maximum action points by @Numbers.ENGDcln(Model.Impact, "point").

@code {
    [Parameter]
    public AccelerationEffect Model { get; set; } = new();
}
