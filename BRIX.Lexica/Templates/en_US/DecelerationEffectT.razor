@using BRIX.Lexica
@using BRIX.Library.Effects

Decreases the @((MarkupString)"target's") maximum action points by @Numbers.ENGDcln(Model.Impact, "point").

@code {
    [Parameter]
    public DecelerationEffect Model { get; set; } = new();
}
