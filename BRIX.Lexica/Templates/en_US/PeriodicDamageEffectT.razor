@using BRIX.Lexica
@using BRIX.Library.Aspects
@using BRIX.Library.Effects

Deals @Numbers.ENGDcln(Model.Impact, "point") damage at the start of the targets turn during @Numbers.ENGDcln(Model.GetAspect<DurationAspect>()?.Duration ?? 0, "round").

@code {
    [Parameter]
    public DamageEffect Model { get; set; } = new();
}
