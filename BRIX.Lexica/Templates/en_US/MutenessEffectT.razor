@using BRIX.Lexica
@using BRIX.Library.Aspects
@using BRIX.Library.Effects

Mute targets for @Numbers.ENGDcln(Model.GetAspect<DurationAspect>
    ().Duration, "round"), if they have  @Numbers.ENGDcln(Model.HealthThreshold, "health") or less.

@code {
    [Parameter]
    public MutenessEffect Model { get; set; } = new();
}
