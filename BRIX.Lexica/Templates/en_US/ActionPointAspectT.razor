@using BRIX.Lexica
@using BRIX.Library.Aspects

To activate ability, you need to spend @Numbers.ENGDcln(Model.ActionPoints, "action point").

@code {
    [Parameter]
    public ActionPointAspect Model { get; set; } = new();
}