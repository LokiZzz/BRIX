<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:BRIX.Mobile.ViewModel.Characters"
             xmlns:controls="clr-namespace:BRIX.Mobile.Resources.Controls"
             xmlns:icons="clr-namespace:BRIX.Mobile.View.IconFonts"
             x:Class="BRIX.Mobile.View.Characters.CurrentCharacterPage"
             x:DataType="vm:CurrentCharacterPageVM"
             Title="Active character">

    <Shell.TitleView>
        <controls:TitleView/>
    </Shell.TitleView>

    <Grid>
        
        <!--In case of empty character pool-->
        <VerticalStackLayout IsVisible="{Binding PlayerHaveCharacter, Converter={StaticResource NotConverter}}">
            <Button Text="Create new character" Command="{Binding CreateCommand}"/>
        </VerticalStackLayout>
        
        <!--In case of player have at leatst one character-->
        <VerticalStackLayout IsVisible="{Binding PlayerHaveCharacter}">
            <Label Text="Name:"/>
            <Label Text="{Binding Character.Name}"/>
            <Label Text="Backstory:"/>
            <Label Text="{Binding Character.Backstory}"/>
            <Label Text="Appearance:"/>
            <Label Text="{Binding Character.Appearance}"/>
            <HorizontalStackLayout>
                <Button Text="Switch" Command="{Binding SwitchCommand}"/>
                <Button Text="Edit" Command="{Binding EditCommand}"/>
            </HorizontalStackLayout>

            <!--<VerticalStackLayout Spacing="5">
                <Label FontSize="24" FontFamily="Garamond" Text="Test — тест"/>
                <Label FontSize="12" FontFamily="SourceSansPro" Text="Тест шрифтов! Сайт рыбатекст поможет дизайнеру, верстальщику, вебмастеру сгенерировать несколько абзацев более менее осмысленного текста рыбы на русском языке, а начинающему оратору отточить навык публичных выступлений в домашних условиях."/>
                <Label FontSize="12" FontFamily="SourceSansPro" Text="Font test! Dictum lacinia cursus platea faucibus. Integer et malesuada nulla efficitur amet, ornare in nunc ut. Nulla mauris ipsum non vitae malesuada pellentesque nec in faucibus. Efficitur ornare pulvinar sed cursus ut. Vestibulum amet est. Nisi adipiscing vestibulum ex."/>
                <Label FontSize="36" FontFamily="Awesome" Text="{x:Static icons:Awesome.Fire}"/>
                <Label FontSize="36" FontFamily="AwesomeBrands" Text="{x:Static icons:AwesomeBrands.Vk}"/>
                <Label FontSize="120" FontFamily="AwesomeRPG" Text="{x:Static icons:AwesomeRPG.Sword}"/>
            </VerticalStackLayout>-->
        </VerticalStackLayout>
            
    </Grid>
</ContentPage>